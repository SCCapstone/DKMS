"use client";

import SidebarMenu from "./SidebarMenu";

const SidebarContainer = () => (
  // todo: refactor how sidebar logic happens

  <SidebarMenu currentSelection="none" onChange={() => undefined} />
);
export default SidebarContainer;
